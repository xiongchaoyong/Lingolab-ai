# 模块流程链路文档

> 逐模块梳理功能流程与技术架构，供答辩和代码审查使用。

---

## 模块 1：用户注册与多维度画像

### 一、功能流程

#### 1.1 注册流程

```
用户打开 /register 页面
  → 填写表单：用户名、邮箱、密码、确认密码、年龄、学习目标、兴趣标签
  → 前端表单验证（正则校验用户名/邮箱/密码格式）
  → 点击「注册并开始测评」
  → 调用 authStore.register()
  → 成功后存 Token 到 localStorage + Pinia
  → 路由跳转到 /assessment（水平测评）
```

#### 1.2 登录流程

```
用户打开 /login 页面
  → 输入用户名 + 密码
  → 点击登录
  → 调用 authStore.login()
  → 后端验证密码 + 检查 is_active
  → 成功后返回 JWT + assessment_completed 状态
  → 前端存 Token，根据 redirect 字段跳转：
      ├─ assessment_completed = false → /assessment
      └─ assessment_completed = true  → /home
```

#### 1.3 个人资料查看与编辑

```
用户打开 /profile 页面
  → onMounted 调用 authStore.fetchProfile() → GET /api/auth/profile
  → 展示只读信息（用户名、邮箱、年龄归类、CEFR 等级）
  → 可编辑字段：学习目标（下拉）、兴趣标签（多选）
  → 点击「保存修改」→ PUT /api/auth/profile
  → 后端乐观锁校验 version 字段，并发安全
```

#### 1.4 Token 自动刷新

```
任意 API 请求返回 401
  → axios 响应拦截器捕获
  → 检查是否已在刷新中（isRefreshing 标志）
  → 若未在刷新：调用 POST /api/auth/token/refresh
  → 后端用当前有效 Token 换取新 Token（24h 有效期）
  → 前端更新 localStorage + 重试原始请求
  → 若刷新失败：清除登录状态，提示重新登录
  → 若已在刷新：请求加入 pendingRequests 队列，等待刷新完成后重试
```

#### 1.5 路由守卫

```
每次路由切换 → router.beforeEach 守卫：
  ├─ 游客路由（/login, /register）+ 已登录 → 重定向 /
  ├─ 需认证路由 + 未登录 → 重定向 /
  ├─ 已完成测评 → 禁止再访问 /assessment → 重定向 /
  ├─ 未完成测评 + 非豁免路径 → 强制跳转 /assessment
  └─ 角色路由（/teacher/*, /admin/*）→ 角色不匹配 → 重定向 /
```

#### 1.6 管理员禁用/恢复账号

```
管理员调用 POST /api/auth/admin/toggle-status
  → 验证当前用户 role == "admin"
  → 不能禁用自己
  → 修改目标用户 is_active 字段
  → 被禁用用户下次请求时 get_current_user 拦截（is_active != 1 → 401）
```

---

### 二、技术架构

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端页面 | Vue 3 + Element Plus | RegisterView / LoginView / ProfileView |
| 前端状态 | Pinia + localStorage | Token 持久化，userInfo 响应式管理 |
| 前端路由 | Vue Router 4 + beforeEach 守卫 | 5 层权限检查（guest/auth/assessment/role） |
| 前端 HTTP | Axios + 请求/响应拦截器 | 自动附带 Token，401 自动刷新 + 重试队列 |
| 后端路由 | FastAPI | `api/auth.py` — 6 个端点（register/login/profile/update/refresh/toggle） |
| 认证 | JWT (python-jose) | HS256 签名，24h 有效期，无状态 |
| 密码安全 | bcrypt (passlib) | 不可逆哈希 + salt |
| 并发控制 | 乐观锁 | user_profiles.version 字段 + WHERE 条件更新 |
| ORM | SQLAlchemy | `UserProfile` 模型映射 `user_profiles` 表（20+ 字段） |
| 校验 | Pydantic | RegisterRequest/LoginRequest/ProfileUpdateRequest 请求校验 |

---

## 模块 3：个性化学习路径规划

### 一、功能流程

#### 3.1 每日任务生成

```
用户登录后进入「学习路径」页面
  → onMounted 调用 store.fetchTasks()
  → GET /api/learning-path/tasks
  → 后端检查 daily_tasks 表今日是否已有任务：
      ├─ 有 → 直接返回已有任务
      └─ 无 → 执行六步生成法：
          ① 短板优先 → 从 user_skill_scores / 测评记录取最低分维度
          ② 目标加权 → learning_goal 映射场景偏好（日常/考试/商务/出国/兴趣）
          ③ CEFR 匹配 → 图查找 BELONGS_TO 同等级的技能节点
          ④ 前置依赖 → 检查技能前置链是否满足（HAS_PREREQ）
          ⑤ 兴趣优选 → 资料标签 ∩ 用户兴趣标签
          ⑥ 写入 daily_tasks → 返回 3 个任务（跟读+对话+听力各 1 个）
  → 前端展示任务卡片列表 + 进度条（done/total）
```

#### 3.2 任务操作

```
跳过任务：
  → POST /api/learning-path/tasks/{id}/skip
  → 更新 status="skipped" + skip_reason
  → 返回更新后的任务

换一个：
  → POST /api/learning-path/tasks/{id}/replace
  → 删除旧任务 → 生成同类型新任务（随机选取同 CEFR 等级的技能/场景/资料）
  → 返回新任务

调整难度：
  → POST /api/learning-path/tasks/{id}/adjust-difficulty（direction: easier/harder）
  → CEFR 等级 ±1 级（钳制 A1-C2）
  → 返回更新后的任务

完成任务：
  → POST /api/learning-path/tasks/{id}/complete（score + duration_seconds）
  → 更新 status="completed" + completed_at
  → profile_updater.ingest_task_score() 摄入分数到动态画像
```

#### 3.3 历史记录

```
用户展开「学习历史」区域
  → GET /api/learning-path/history?days=7
  → 后端查询最近 N 天的 daily_tasks
  → 按日期分组：每日的任务状态序列 + 完成数 + 总数 + 学习分钟数
  → 前端展示日历列表（done/skipped/pending 图标 + 统计）
```

#### 3.4 资料推荐

```
用户进入「资料推荐」页面
  → GET /api/recommendations/
  → 后端执行四因子评分推荐：
      ① 短板匹配 (40%) → 资料 TEACHES 的技能是否属于用户短板维度
      ② 难度匹配 (35%) → 资料 CEFR 与用户 level_final 差值
      ③ 兴趣匹配 (25%) → 资料标签 ∩ 用户兴趣标签
      ④ 新颖度（乘性因子）→ 7 天内推荐过打折，disliked 归零
  → 按分数降序，取视频×2 + 文章×2 + 音频×2
  → 写入 material_recommendations 表
  → 返回 6 条推荐资料
```

#### 3.5 资料交互

```
用户点击「不感兴趣」：
  → POST /api/recommendations/{id}/dislike
  → 更新 action="disliked"
  → 后续推荐该资料新颖度归零，不再出现

用户点击资料：
  → POST /api/recommendations/{id}/click（action: view/complete）
  → 更新 action + viewed_at

用户点击「换一批」：
  → POST /api/recommendations/refresh
  → 每日限 3 次手动刷新（首次加载不计入）
  → 超限返回 HTTP 429
  → 重新计算四因子评分 → 写入新推荐记录
```

### 二、技术架构

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端页面 | Vue 3 + Element Plus | LearningPathView（任务卡片） + RecommendationView（资料列表） |
| 前端状态 | Pinia Store | `learning_path.js` — 任务/进度/操作状态管理 |
| 前端路由 | Vue Router | `/learning-path` + `/recommendations` |
| 后端路由 | FastAPI | `api/learning_path.py` — 6 个端点（tasks/skip/replace/adjust/complete/history） |
| 后端路由 | FastAPI | `api/recommendation.py` — 4 个端点（GET //dislike/refresh/click） |
| 知识图谱 | NetworkX DiGraph | 129 节点 + 292 边，启动时从 MySQL 全量加载到内存 |
| 图遍历 | BFS + 拓扑排序 | `get_prerequisite_chain()` 收集前置依赖 → `nx.topological_sort()` |
| 推荐算法 | 四因子评分 | 短板 40% + 难度 35% + 兴趣 25%，乘以新颖度因子 |
| 任务生成 | 六步规则引擎 | 短板→目标→CEFR→前置依赖→兴趣→写入 DB |
| 画像更新 | ProfileUpdater | 任务完成后 EMA 摄入分数到 user_skill_scores |
| ORM | SQLAlchemy | KGNode / KGEdge / DailyTask / MaterialRecommendation / LearningMaterial / MaterialRecord |
| 校验 | Pydantic | TaskItem / DailyTasksResponse / MaterialItem / RecommendationsResponse |
## 模块 2：英语水平智能测评

### 一、功能流程

#### 2.1 开始测评

```
用户注册成功后自动跳转 /assessment
  → 路由守卫检查：已完成测评 → 禁止重测 → 重定向 /
  → onMounted 调用 store.startAssessment()
  → 先检查 localStorage 是否有未完成进度：
      ├─ 有进度 → 调用 POST /api/assessment/restore 恢复后端会话
      │   ├─ 恢复成功 → 继续上次题目
      │   └─ 恢复失败（会话过期）→ 清除旧数据，走新建流程
      └─ 无进度 → 调用 POST /api/assessment/start 创建新会话
  → 后端返回 session_id + 第一题 + 总题数 + 当前难度
  → 前端保存进度到 localStorage
```

#### 2.2 客观题答题

```
用户看到题目（听力/阅读/语法）
  → 题目从 DB 按维度 + CEFR 随机抽取
  → 用户选择 A/B/C/D 选项
  → 点击「确认答案」
  → 调用 POST /api/assessment/answer（FormData: session_id, question_id, answer）
  → 后端比对正确选项 → 判分（100/0）
  → 累计维度分数 → 自适应调整难度（≥60 升 0.5 级，<60 降 0.5 级）
  → 返回下一题 + 新难度等级
  → 前端更新题目展示 + 进度条
```

#### 2.3 口语题答题

```
用户看到口语题
  → 前端展示 VoiceRecorder 组件（3 秒准备 + 最长 45 秒录音）
  → 用户录音 → 完成后触发 handleSpeakingComplete
  → 调用 POST /api/assessment/answer（FormData: session_id, question_id, answer="", audio 文件）
  → 后端处理：
      ├─ 保存音频到临时文件
      ├─ ffmpeg 转码为 WAV 16kHz
      ├─ WhisperX 转写 → 文本 transcript
      ├─ LLM（DashScope qwen-plus）四维评分（内容/语法/词汇/连贯，各 0-25）
      ├─ 保存音频到 uploads/assessment/{session_id}/
      └─ 返回得分 + 下一题
  → 前端显示「评分中...」加载状态
```

#### 2.4 全对/全错追加题

```
基础 10 题答完
  → 后端检查所有题目得分：
      ├─ 10 题全答对（得分≥60）→ 追加 1 道 C2 口语确认题
      ├─ 10 题全答错（得分<60）→ 追加 1 道 A1 听力基础题
      └─ 正常情况 → 直接结束
  → 追加题不影响基础 10 题的维度分数计算
```

#### 2.5 完成测评

```
所有题目答完（含追加题）
  → 调用 POST /api/assessment/complete
  → 后端计算：
      ├─ 四维均分（听力/口语/阅读/语法各维度平均）
      ├─ 综合分 = 四维均分的平均
      ├─ CEFR 定级（A1:0-20 / A2:21-40 / B1:41-60 / B2:61-80 / C1:81-95 / C2:96-100）
      ├─ 短板维度（最低分维度 + 学习建议）
      └─ 更新 user_profiles：level_test + level_final + assessment_completed=1
  → 前端清除 localStorage 进度
  → 跳转 /assessment/result 展示报告
```

#### 2.6 中途退出与恢复

```
用户点击「退出」或关闭页面
  → store.saveProgress() 保存到 localStorage（session_id + 当前题目 + 已答数 + 难度）
  → 下次进入 /assessment → startAssessment() 检测到 localStorage 有进度
  → 调用 POST /api/assessment/restore
  → 后端从 assessment_records 表重建会话状态（反推已答题目 + 当前难度）
  → 继续从上次中断的题目开始
```

### 二、技术架构

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端页面 | Vue 3 + Element Plus | AssessmentView（答题页） + AssessmentResult（结果页） |
| 前端组件 | VoiceRecorder | 录音组件（MediaRecorder API，WebM 格式） |
| 前端状态 | Pinia Store | `assessment.js` — 会话/题目/进度/评分状态管理 |
| 前端持久化 | localStorage | 中断恢复：session_id + 当前题目 + 进度 |
| 后端路由 | FastAPI | `api/assessment.py` — 5 个端点（start/answer/complete/restore/submit） |
| 会话管理 | 内存字典 + DB 双写 | `_sessions` dict 存活跃会话，assessment_records 持久化 |
| 自适应算法 | CEFR 数值映射 | A1-C2 → 1.0-6.0，得分≥60 升 0.5 级，钳制 [1.0, 6.0] |
| ASR | WhisperX small (int8) | 口语题语音转文字 |
| LLM 评分 | DashScope qwen-plus | 口语题四维评分（内容/语法/词汇/连贯） |
| 音频处理 | ffmpeg + 转码 | WebM → WAV 16kHz 单声道 |
| 题库 | MySQL assessment_questions | 30 题（A2:9/B1:11/B2:10），按维度+难度分类 |
| ORM | SQLAlchemy | AssessmentQuestion + AssessmentRecord 模型 |
| 校验 | Pydantic | QuestionItem / AssessmentStartResponse / AssessmentAnswerResponse |

---

## 模块 4：智能语音对话练习

### 一、功能流程

#### 4.1 开始对话

```
用户进入「AI 对话」页面
  → 选择场景（自我介绍/问路/购物/餐厅）
  → 点击「开始对话」
  → 调用 POST /api/conversation/start（或 /stream/start 流式版）
  → 后端：
      ├─ 创建内存会话（_sessions dict）+ DB 会话（conversation_sessions）
      ├─ 根据场景生成 AI 开场白 Prompt
      ├─ LLM（DashScope qwen-plus）生成开场白
      └─ 返回 session_id + AI 开场白文本
  → 流式版：SSE 逐 token 返回 + 后台 TTS 预取
  → 前端展示对话界面 + 播放 AI 语音
```

#### 4.2 用户说话

```
用户点击麦克风录音
  → 前端 VoiceRecorder 录音（MediaRecorder API，WebM 格式）
  → 录音完成 → 上传音频到 POST /api/conversation/speak（或 /stream/speak）
  → 后端处理管线：
      ├─ ffmpeg 转码 WebM → WAV 16kHz
      ├─ WhisperX ASR 转写 → 文本 + 词级时间戳
      ├─ 算法流利度计算（维度1-3：wpm/停顿频率/重复率，静默存储）
      ├─ LLM 语法纠错（asyncio.create_task 并行，不增加延迟）
      ├─ LLM 生成回复（chat_stream SSE 逐 token）
      ├─ 持久化用户消息 + AI 回复到 conversation_messages
      └─ 后台 TTS 预取（Edge TTS 缓存音频块）
  → 前端 SSE 接收：
      ├─ asr 事件 → 显示用户转写文本
      ├─ token 事件 → 逐字显示 AI 回复
      ├─ grammar 事件 → 显示语法纠错卡片（可折叠）
      └─ done 事件 → 对话完成标记 + TTS 音频 URL
  → 最多 10 轮（MAX_CONVERSATION_ROUNDS = 10）
```

#### 4.3 TTS 语音播放

```
前端获取 AI 回复后
  → 请求 GET /api/conversation/tts/cached/{session_id}/{round_key}
  → 后端从内存 TTS 缓存流式返回 MP3
  → 前端 <audio> 播放，边下载边播放
  → 若缓存未就绪，轮询等待（50ms 间隔）
```

#### 4.4 结束对话与评分

```
达到 10 轮或用户点击「结束对话」
  → 调用 POST /api/conversation/end
  → 后端双层评分体系：
      ├─ 语音维度（wav2vec2）：
      │   对每段用户音频评测五维（音素准确度/重音/语调/连读/节奏）
      │   → 各维度取平均
      ├─ 文本维度（LLM）：
      │   语法正确率 + 词汇丰富度 + 对话参与度（各含 feedback/strengths/weaknesses）
      ├─ 综合分 = 语音均分 × 50% + 文本均分 × 50%
      ├─ 流利度评估：
      │   算法维度（wpm/停顿/重复）+ LLM 维度（语法/相关性）
      │   → 五维汇总 → 分段评级（优秀/良好/中等/初级/入门）
      └─ 持久化：更新 conversation_sessions 评分 + ProfileUpdater 摄入分数
  → 前端展示综合报告（综合分→语音评测→文本评测→流利度→对话记录→改进建议）
```

### 二、技术架构

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端页面 | Vue 3 + Element Plus | VoiceCallView（全屏对话界面） |
| 前端组件 | VoiceRecorder | MediaRecorder API 录音，WebM 格式 |
| 前端状态 | SSE EventSource | 流式接收 AI 回复（token/grammar/done 事件） |
| 后端路由 | FastAPI | `api/conversation.py` — 8 个端点（start/speak/stream-start/stream-speak/tts/tts-stream/tts-cached/end/sessions） |
| 会话管理 | 内存字典 + DB 双写 | `_sessions` dict 存活跃会话 + conversation_sessions 持久化 |
| ASR | WhisperX small (int8) | 语音转文字 + 词级时间戳 |
| LLM | DashScope qwen-plus | 对话生成 / 语法纠错 / 流利度评分 / 对话评分 |
| TTS | Edge TTS (Jenny) | 后台预取 + 流式缓存返回 |
| 音频处理 | ffmpeg + audio_utils | WebM → WAV 16kHz 转码 |
| 流利度 | 算法 + LLM 混合 | 语速/停顿/重复（算法）+ 语法/相关性（LLM），满分 100 |
| 评分体系 | wav2vec2 + LLM | 语音五维 + 文本三维，综合分 = 各 50% |
| ORM | SQLAlchemy | ConversationSession + ConversationMessage |
| 校验 | Pydantic | ConversationStartResponse / ConversationSpeakResponse / ConversationEndResponse |

---

## 模块 5：流利度与完整性评估

### 一、功能流程

#### 5.1 每轮流利度静默评分

```
用户每说完一轮（POST /api/conversation/stream/speak）
  → ASR 转写完成后，自动触发算法流利度计算：
      ├─ 维度1 语速(wpm)：单词数 / 说话时长（分钟），满分 25
      │   理想范围 100-120 wpm，偏差按比例扣分
      ├─ 维度2 停顿频率：词间 gap > 0.5s 计为停顿，满分 20
      │   <2次/分钟满分，<5次良好，>10次差
      └─ 维度3 重复率：连续重复词 + 2-gram 重复，满分 20
          <5%满分，<10%良好，>20%差
  → 结果静默存储到内存会话 fluency_scores[]
  → 同时持久化到 conversation_messages.fluency_scores 列
  → 用户无感知，不增加对话延迟
```

#### 5.2 对话结束流利度汇总

```
对话结束（POST /api/conversation/end）
  → 后端汇总所有轮次的流利度数据：
      ├─ 算法维度（已完成）：语速/停顿/重复 各轮平均
      ├─ LLM 维度（对话结束时调用）：
      │   维度4 语法正确性（满分 20）— LLM 逐轮评估语法错误
      │   维度5 内容相关性（满分 15）— LLM 评估回答是否切题
      ├─ 五维综合分 = 各轮（语速+停顿+重复+语法+相关性）的平均
      ├─ 分段评级：85-100 优秀 / 70-84 良好 / 55-69 中等 / 40-54 初级 / <40 入门
      └─ 最佳轮次识别 + 维度平均统计
  → 返回流利度报告：
      {
        overall: 78,
        grade: "良好",
        rounds: [{round, text, wpm, pause, rep, grammar, relevance, total}, ...],
        best_round: 3,
        dimension_averages: {wpm_avg, pause_avg, repetition_avg},
        suggestions: "..."
      }
```

#### 5.3 流利度报告展示

```
前端综合报告页（ConversationEndResponse.fluency）
  → 展示：
      ├─ 综合分 + 评级标签
      ├─ 五维雷达图（语速/停顿/重复/语法/相关性）
      ├─ 逐轮详情卡片（每轮五维得分 + 文本）
      ├─ 最佳轮次高亮
      └─ 改进建议
```

### 二、技术架构

| 层次 | 技术 | 说明 |
|------|------|------|
| 算法引擎 | fluency.py | 三个纯函数：assess_algorithmic() + aggregate_fluency() + _detect_repetitions() |
| 语速计算 | ASR 词级时间戳 | WhisperX words[] → 首词 start 到末词 end = 说话时长 |
| 停顿检测 | 词间 gap | words[i].start - words[i-1].end > 0.5s → 停顿 |
| 重复检测 | 连续重复 + 2-gram | 相邻重复词 + 3-gap 内重复 bigram |
| LLM 评分 | DashScope qwen-plus | score_fluency() 评估语法正确性 + 内容相关性 |
| 持久化 | SQLAlchemy | conversation_messages.fluency_scores (JSON) |
| 满分 | 100 | 语速 25 + 停顿 20 + 重复 20 + 语法 20 + 相关性 15 |

---

## 模块 6：AI 语法纠错与润色

### 一、功能流程

#### 6.1 独立文本纠错

```
用户打开「语法纠错」页面
  → 选择输入模式（文本/语音）+ CEFR 等级
  → 文本模式：输入英文句子 → 点击「检查语法」
  → 调用 POST /api/grammar/correct（FormData: text, cefr_level）
  → 后端处理：
      ├─ 构建 CEFR 感知的纠错 Prompt（含错误类型枚举 + JSON 输出格式）
      ├─ DashScope qwen-plus（temperature=0.3）生成纠错结果
      ├─ 解析 JSON：corrected_text / errors[] / polished_version / suggestions[]
      └─ 返回纠错结果
  → 前端展示：
      ├─ 原文 vs 修正版对比卡片（差异词高亮：红删绿增）
      ├─ 错误列表（类型标签 + 颜色编码 + 原文→修正 + 中文解释）
      ├─ 地道表达建议（润色版本）
      └─ 改进建议（2-4 条）
```

#### 6.2 独立语音纠错

```
用户切换到语音模式
  → VoiceRecorder 录音（3 秒准备 + 最长 30 秒）
  → 录音完成 → 上传音频到 POST /api/grammar/correct/voice
  → 后端处理管线：
      ├─ 保存音频到临时文件
      ├─ ffmpeg 转码为 WAV 16kHz
      ├─ WhisperX ASR 转写 → 文本 transcript
      ├─ 调用 correct_grammar(transcript, cefr_level)
      └─ 返回纠错结果（同文本模式）
  → 前端展示同文本模式
```

#### 6.3 对话内实时纠错

```
用户在 AI 对话中说完一轮（POST /api/conversation/stream/speak）
  → ASR 转写完成后：
      ├─ asyncio.create_task(llm.correct_grammar(user_text, cefr_level))  ← 并行执行
      ├─ 同时 LLM 生成对话回复（不等待纠错完成）
      ├─ SSE 事件流：
      │   ├─ asr 事件 → 显示用户转写文本
      │   ├─ token 事件 → 逐字显示 AI 回复
      │   ├─ grammar 事件 → 显示语法纠错结果（如有错误）
      │   └─ done 事件 → 对话完成
      └─ 纠错结果持久化到 conversation_messages.grammar_check 列
  → 前端展示可折叠语法纠错卡片（错误类型标签 + 中英文解释）
```

#### 6.4 角色扮演内实时纠错

```
同对话内纠错流程，集成在 roleplay.py 的 stream/speak 端点中
  → 纠错结果通过 SSE grammar 事件返回
  → 持久化到 roleplay_messages.grammar_check 列
```

### 二、技术架构

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端页面 | Vue 3 + Element Plus | GrammarView（双模式输入 + 差异高亮 + 错误卡片） |
| 前端组件 | VoiceRecorder | MediaRecorder API 录音，WebM 格式 |
| 前端 API | Axios | `api/grammar.js` — correctGrammar / correctGrammarVoice |
| 后端路由 | FastAPI | `api/grammar.py` — 2 个端点（POST /correct, POST /correct/voice） |
| LLM | DashScope qwen-plus | temperature=0.3，8 种错误类型，CEFR 感知提示 |
| ASR | WhisperX small (int8) | 语音纠错时转写音频 |
| 音频处理 | ffmpeg | WebM → WAV 16kHz 转码 |
| 对话集成 | asyncio.create_task | 与 LLM 回复并行执行，不增加延迟 |
| SSE 事件 | grammar 类型 | 纠错结果通过 SSE 流式返回前端 |
| ORM | SQLAlchemy | conversation_messages.grammar_check / roleplay_messages.grammar_check |
| 校验 | Pydantic | GrammarError / GrammarCorrectResponse |
| 错误类型 | 8 种 | tense / subject_verb_agreement / article / preposition / word_order / plural / word_choice / other |

---

## 模块 7：情景角色扮演

### 一、功能流程

#### 7.1 开始角色扮演

```
用户进入「角色扮演」页面
  → 选择角色（面试者 interviewee / 服务员 waiter / 导游 guide）+ CEFR 等级
  → 点击「开始角色扮演」
  → 调用 POST /api/roleplay/stream/start（流式版）
  → 后端：
      ├─ 创建内存会话（_sessions dict）+ DB 会话（conversation_sessions，scene=free）
      ├─ 根据角色选择 ROLE_PROMPTS（面试官/顾客/游客视角）
      ├─ LLM（DashScope qwen-plus, temperature=0.9）流式生成 AI 开场白
      ├─ SSE 逐 token 返回 → 前端实时显示
      └─ 后台 TTS 预取（Edge TTS Jenny 缓存音频块）
  → 前端展示角色对话界面 + 播放 AI 语音
```

#### 7.2 用户说话

```
用户点击麦克风录音
  → VoiceRecorder 录音（MediaRecorder API，WebM 格式）
  → 录音完成 → 上传音频到 POST /api/roleplay/stream/speak
  → 后端处理管线：
      ├─ ffmpeg 转码 WebM → WAV 16kHz
      ├─ WhisperX ASR 转写 → 文本 + 词级时间戳
      ├─ 算法流利度计算（语速/停顿/重复，静默存储到内存会话）
      ├─ LLM 语法纠错（asyncio.create_task 并行，不增加延迟）
      ├─ LLM 角色回复（chat_roleplay_stream SSE 逐 token）
      ├─ 持久化用户消息 + AI 回复到 conversation_messages
      └─ 后台 TTS 预取（Edge TTS 缓存音频块）
  → 前端 SSE 接收：
      ├─ asr 事件 → 显示用户转写文本
      ├─ token 事件 → 逐字显示 AI 角色回复
      ├─ grammar 事件 → 显示语法纠错卡片（可折叠）
      └─ done 事件 → 对话完成标记 + TTS 音频 URL
  → 最多 6 轮（MAX_CONVERSATION_ROUNDS = 6）
```

#### 7.3 TTS 语音播放

```
前端获取 AI 回复后
  → 请求 GET /api/roleplay/tts/cached/{session_id}/{round_key}
  → 后端从内存 TTS 缓存流式返回 MP3
  → 前端 <audio> 播放，边下载边播放
  → 若缓存未就绪，轮询等待（50ms 间隔）
```

#### 7.4 结束角色扮演与评分

```
达到 6 轮或用户点击「结束对话」
  → 调用 POST /api/roleplay/end
  → 后端双层评分体系：
      ├─ 语音维度（wav2vec2）：
      │   对每段用户音频评测五维（音素准确度/重音/语调/连读/节奏）
      │   → 各维度取平均
      ├─ 角色维度（LLM score_roleplay）：
      │   角色贴合度(40%) + 场景礼仪(25%) + 专业术语(20%) + 应对能力(15%)
      │   → 各维度含 feedback/strengths/weaknesses 详评
      ├─ 综合分 = 角色加权分 × 60% + 语音均分 × 40%
      ├─ 流利度评估：
      │   算法维度（wpm/停顿/重复）+ LLM 维度（语法/相关性）
      │   → 五维汇总 → 分段评级
      └─ 持久化：更新 conversation_sessions 评分 + ProfileUpdater 摄入分数
  → 前端展示综合报告（综合分→角色四维→语音五维→流利度→对话记录→改进建议）
```

### 二、技术架构

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端页面 | Vue 3 + Element Plus | RolePlayView（全屏角色对话界面） |
| 前端组件 | VoiceRecorder | MediaRecorder API 录音，WebM 格式 |
| 前端状态 | SSE EventSource | 流式接收 AI 回复（token/grammar/done 事件） |
| 后端路由 | FastAPI | `api/roleplay.py` — 8 个端点（start/speak/stream-start/stream-speak/tts/tts-stream/tts-cached/end） |
| 会话管理 | 内存字典 + DB 双写 | `_sessions` dict 存活跃会话 + conversation_sessions 持久化 |
| ASR | WhisperX small (int8) | 语音转文字 + 词级时间戳 |
| LLM | DashScope qwen-plus | 角色对话生成（temperature=0.9）/ 四维评分（temperature=0.3）/ 语法纠错 / 流利度评分 |
| TTS | Edge TTS (Jenny) | 后台预取 + 流式缓存返回 |
| 音频处理 | ffmpeg + audio_utils | WebM → WAV 16kHz 转码 |
| 角色 Prompt | 3 个内置场景 | interviewee（面试官视角）/ waiter（顾客视角）/ guide（游客视角） |
| 评分体系 | wav2vec2 + LLM | 角色四维 × 60% + 语音五维 × 40%，综合分加权 |
| 流利度 | 算法 + LLM 混合 | 语速/停顿/重复（算法）+ 语法/相关性（LLM），满分 100 |
| ORM | SQLAlchemy | ConversationSession + ConversationMessage（复用对话模块表） |
| 校验 | Pydantic | RoleplayStartRequest / RoleplayStartResponse / RoleplaySpeakResponse / RoleplayEndResponse |

---

## 模块 8：AI 发音评测与纠错

### 一、功能流程

#### 8.1 发音评测

```
用户打开「发音评测」页面
  → 选择跟读内容（从内容库加载，支持按类型/难度筛选）
  → 或自由输入文本跟读
  → 点击麦克风录音 → VoiceRecorder 录音（MediaRecorder API，WebM 格式）
  → 录音完成 → 上传音频到 POST /api/pronunciation/score（FormData: audio, text, mode）
  → 后端处理管线：
      ├─ 保存音频到临时文件
      ├─ ffmpeg 转码为 WAV 16kHz 单声道
      ├─ WhisperX ASR 转写 + 词级/音素级时间戳对齐
      ├─ wav2vec2 + CTC 强制对齐 → 音素级 GOP 得分
      ├─ 五维评分计算：
      │   ├─ 音素准确度：各音素 GOP 得分平均
      │   ├─ 重音位置：librosa RMS 能量包络检测重音变化
      │   ├─ 语调曲线：librosa PYIN 提取 F0 基频 + 线性趋势拟合
      │   ├─ 连读表现：WhisperX 词级时间戳 + G2P 辅元连读条件检测 + gap 评分
      │   └─ 节奏感：CTC 对齐音素时长分布 + 变异系数 CV + 异常停顿检测
      ├─ 模式加权综合分：
      │   ├─ 单词模式：音素 50% + 重音 25% + 节奏 25%
      │   └─ 句子模式：音素 40% + 重音 15% + 连读 15% + 语调 15% + 节奏 15%
      ├─ 持久化：评测记录写入 pronunciation_records + 画像分数摄入
      └─ 返回：综合分 + 五维评分 + 错误音素 + 可视化数据
  → 前端展示：
      ├─ 综合分 + 五维评分卡片
      ├─ 详情弹窗（5 个 Tab）：
      │   ├─ 逐音素评分表（色块 + GOP 分数 + 评级）
      │   ├─ 重音位置分析（能量柱状图 + 重音标记）
      │   ├─ 语调曲线分析（F0 曲线 + 趋势线 + 句型判断）
      │   ├─ 连读表现（词对表格 + gap 色标 + 连读条件）
      │   └─ 节奏感（音素时长柱状图 + 异常停顿标红）
      └─ 标准发音对比（Edge TTS 生成参考音频，双播放器并排）
```

#### 8.2 跟读内容库

```
用户进入发音评测页面
  → 调用 GET /api/pronunciation/content（可选 content_type + cefr_level 筛选）
  → 后端查询 pronunciation_content 表（is_active=1）
  → 返回内容列表（标题/文本/类型/难度/分类/IPA 音标）
  → 前端展示内容卡片列表，点击选择跟读内容
```

#### 8.3 评测历史

```
用户点击「历史记录」
  → 调用 GET /api/pronunciation/records（limit=20）
  → 后端查询当前用户的 pronunciation_records（按时间倒序）
  → 关联查询 pronunciation_content 获取内容标题
  → 前端展示历史记录列表（分数/模式/时间/内容标题）
```

#### 8.4 参考音频对比

```
评测完成后，用户点击「查看标准发音」
  → 并行请求 POST /api/pronunciation/reference-audio（Edge TTS Jenny 生成标准音）
  → 后端 Edge TTS 合成 MP3 音频流返回
  → 前端并排显示双播放器（你的录音 VS 标准发音）
```

### 二、技术架构

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端页面 | Vue 3 + Element Plus | PronunciationView（跟读内容 + 录音 + 评分 + 详情弹窗） |
| 前端组件 | VoiceRecorder | MediaRecorder API 录音，WebM 格式 |
| 前端 API | Axios | `api/pronunciation.js` — scorePronunciation / getContentList / getRecordList |
| 后端路由 | FastAPI | `api/pronunciation.py` — 4 个端点（score/reference-audio/content/records） |
| 发音评测 | wav2vec2 + GOP + CTC | 音素级 GOP 得分，MPS/CPU 自适应 |
| ASR 对齐 | WhisperX small (int8) | 词级/音素级时间戳，用于连读/节奏分析 |
| 重音检测 | librosa RMS | 能量包络检测重音变化 |
| 语调分析 | librosa PYIN | F0 基频提取 + 线性趋势拟合 |
| 连读检测 | WhisperX + G2P | 词级时间戳 + 辅元连读条件 + gap 评分 |
| 节奏分析 | CTC 对齐 | 音素时长分布 + 变异系数 CV + 异常停顿 |
| G2P | g2p-en | 音素→音标映射 |
| TTS 参考音 | Edge TTS (Jenny) | 生成标准发音 MP3 对比 |
| 持久化 | SQLAlchemy | pronunciation_content + pronunciation_records |
| 画像更新 | ProfileUpdater | ingest_pronunciation_scores() 摄入分数到 user_skill_scores |
| 校验 | Pydantic | PronunciationResponse / ContentItem / RecordItem + 8 个可视化 Schema |

---

## 模块 10：游戏化闯关学习

### 一、功能流程

#### 10.1 每日闯关

```
用户打开「闯关学习」页面
  → 调用 GET /api/gamification/daily-challenge
  → 后端返回 5 关递增难度句子（A1→B2）+ 已完成关卡状态
  → 用户点击关卡 → 录音跟读
  → 提交到 POST /api/gamification/daily-challenge/submit
  → 后端：ffmpeg 转码 → 发音评测（复用 pronunciation service）→ 判分（≥70 通过）
  → 通过后自动进入下一关
  → 全部完成 → POST /api/gamification/daily-challenge/complete
  → 后端：发放积分（每关 20 + 全通额外 30）+ 检查勋章条件 + 更新画像
```

#### 10.2 配音挑战

```
用户切换到「配音挑战」Tab
  → 调用 GET /api/gamification/dubbing/content（可选难度筛选）
  → 选择片段 → 录音配音
  → 提交到 POST /api/gamification/dubbing/submit
  → 后端：发音评测 + 三维评分（发音相似度/语调相似度/情感匹配度）→ 综合分
  → 发放积分（≥85 额外奖励）+ 检查勋章
```

#### 10.3 积分与勋章

```
GET /api/gamification/points → 积分总览 + 最近积分记录
GET /api/gamification/badges → 勋章列表（含未获得的灰色状态）
GET /api/gamification/leaderboard → 积分排行榜（Top N）
```

### 二、技术架构

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端页面 | Vue 3 + Element Plus | ChallengeView（闯关 + 配音 + 积分 + 勋章 + 排行榜） |
| 后端路由 | FastAPI | `api/gamification.py` — 9 个端点 |
| 服务层 | GamificationService | 每日闯关 5 关 + 配音评分 + 积分发放 + 勋章检查 |
| 积分规则 | POINTS_RULES | 8 种行为积分（任务/闯关/配音/打卡/发音/分享） |
| 勋章系统 | BADGE_DEFINITIONS | 7 枚勋章（新手/坚持/发音/进步/配音/满分/学霸） |
| 发音评测 | wav2vec2 + GOP | 复用 pronunciation service |
| ORM | SQLAlchemy | UserScore / UserBadge / DubbingContent / DubbingRecord |
| 校验 | Pydantic | 10 个 Schema（闯关/配音/积分/勋章/排行榜） |

---

## 模块 12：学习进度可视化追踪

### 一、功能流程

#### 12.1 雷达图

```
用户打开「学习进度」页面
  → 调用 GET /api/progress/radar?range=week
  → 后端查询 user_skill_scores 表，按五维（发音准确率/流利度/语法/听力/表达丰富度）聚合 EMA 均值
  → 返回当前周期 + 上周期对比数据
  → 前端 ECharts 雷达图渲染
```

#### 12.2 趋势折线图

```
调用 GET /api/progress/trend?range=week
  → 后端按日期聚合发音分 + 流利度分
  → 返回每日数据点
  → 前端 ECharts 折线图渲染
```

#### 12.3 日历热力图

```
调用 GET /api/progress/heatmap?year=2026
  → 后端查询全年每日活动次数
  → 返回 365 天数据（count + 颜色等级 0-3）
  → 前端日历热力图渲染
```

#### 12.4 统计卡片

```
调用 GET /api/progress/stats
  → 后端查询 6 项核心指标（总学习时长/完成任务数/连续打卡/当前等级/发音均分/积分）
  → 前端统计卡片展示
```

### 二、技术架构

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端页面 | Vue 3 + ECharts | ProgressView（雷达图 + 折线图 + 热力图 + 统计卡片） |
| 后端路由 | FastAPI | `api/progress.py` — 4 个端点（radar/trend/heatmap/stats） |
| 服务层 | ProgressService | 数据聚合：五维 EMA + 趋势 + 热力图 + 统计 |
| 五维维度 | RADAR_DIMENSIONS | 发音准确率 / 流利度 / 语法 / 听力 / 表达丰富度 |
| ORM | SQLAlchemy | UserSkillScore / DailyTask / DubbingRecord |
| 校验 | Pydantic | RadarResponse / TrendResponse / HeatmapResponse / StatsResponse |

---

## 模块 13：学习效果预测与预警

### 一、功能流程

#### 13.1 学习预测

```
用户打开进度页的「预测」区域
  → 调用 GET /api/prediction/current
  → 后端线性回归计算：
      ├─ 取最近 N 天的综合分数据
      ├─ 拟合趋势线 → 趋势斜率（分/天）
      ├─ 根据目标分数计算预计达标天数/日期
      └─ 判断趋势方向（up/down/stable）
  → 返回预测数据 + 说明文字
```

#### 13.2 目标设置

```
用户调整目标分数
  → PUT /api/prediction/target（body: target_score）
  → 后端更新 learning_predictions 表 → 重新计算预测
```

#### 13.3 预警检查

```
调用 GET /api/prediction/alerts
  → 后端检查 3 条预警规则：
      ├─ 连续 3 天未学习 → warning
      ├─ 发音分连续下降 → warning
      └─ 即将达标提醒 → info
  → 返回预警列表（type/title/message/level/triggered）
```

#### 13.4 通知系统

```
GET /api/notices → 通知列表（prediction/alert/achievement 类型）+ 未读数
PUT /api/notices/{id}/read → 标记单条已读
PUT /api/notices/read-all → 全部已读
GET /api/notices/unread-count → 未读数量（TopNav 通知铃铛 60s 轮询）
```

### 二、技术架构

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端页面 | Vue 3 + ECharts | ProgressView 内嵌预测卡片 + 预警面板 |
| 后端路由 | FastAPI | `api/prediction.py` — 6 个端点 |
| 预测算法 | 线性回归 | 最小二乘法拟合趋势线 → 预测达标日期 |
| 预警规则 | 3 条规则 | 连续未学习 / 分数下降 / 即将达标 |
| 通知系统 | notices 表 | 三种类型（prediction/alert/achievement）+ 已读状态 |
| ORM | SQLAlchemy | LearningPrediction / Notice |
| 校验 | Pydantic | PredictionData / AlertItem / NoticeItem / NoticesResponse |

---

## 模块 11：学习社区与社交互动

### 一、功能流程

#### 11.1 语音挑战广场

```
用户打开「社区」页面
  → 调用 GET /api/community/challenges
  → 后端查询进行中的挑战列表（标题/描述/示例文本/截止日期/参与人数）
  → 用户选择挑战 → 录音跟读
  → 提交到 POST /api/community/challenges/{id}/submit
  → 后端：保存录音 → 发音评分 → 写入 challenge_records → 返回提交结果 + 排名
  → 查看排行榜：GET /api/community/challenges/{id}/leaderboard
```

#### 11.2 话题讨论区

```
获取帖子列表：GET /api/community/posts
  → 后端查询 discussion_topics（含点赞数/评论数/当前用户是否已点赞）

发帖：POST /api/community/posts（topic + content）
  → 写入 discussion_topics 表

点赞：POST /api/community/posts/{id}/like
  → 切换点赞状态（已赞则取消）→ 返回 liked + likes_count

评论：GET /api/community/posts/{id}/comments → 评论列表
     POST /api/community/posts/{id}/comments（content）→ 发表评论
```

#### 11.3 学习小组

```
获取小组列表：GET /api/community/groups
  → 后端查询 groups + group_members（含成员数/当前用户是否已加入）

加入/退出：POST /api/community/groups/{id}/join
  → 切换加入状态 → 返回 joined + member_count
```

### 二、技术架构

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端页面 | Vue 3 + Element Plus | CommunityView（挑战广场 + 讨论区 + 学习小组） |
| 前端 API | Axios | `api/community.js` — 10 个函数 |
| 后端路由 | FastAPI | `api/community.py` — 9 个端点 |
| 服务层 | CommunityService | 挑战/帖子/评论/点赞/小组 CRUD |
| 发音评测 | wav2vec2 + GOP | 复用 pronunciation service |
| ORM | SQLAlchemy | challenge_topics / challenge_records / discussion_topics / discussion_comments / groups / group_members |
| 校验 | Pydantic | 16 个 Schema（挑战/帖子/评论/点赞/小组） |

---

## 模块 9：学习资料智能推荐

### 一、功能流程

```
用户进入「资料推荐」页面
  → GET /api/recommendations/
  → 后端执行四因子评分推荐：
      ① 短板匹配 (40%) → 资料 TEACHES 的技能是否属于用户短板维度
      ② 难度匹配 (35%) → 资料 CEFR 与用户 level_final 差值
      ③ 兴趣匹配 (25%) → 资料标签 ∩ 用户兴趣标签
      ④ 新颖度（乘性因子）→ 7 天内推荐过打折，disliked 归零
  → 按分数降序，取视频×2 + 文章×2 + 音频×2
  → 写入 material_recommendations 表
  → 返回 6 条推荐资料

用户交互：
  → 「不感兴趣」：POST /{id}/dislike → 后续推荐不再出现
  → 「换一批」：POST /refresh → 每日限 3 次，超限返回 429
  → 点击/完成：POST /{id}/click → 记录 viewed/completed 状态
```

| 层次 | 技术 | 说明 |
|------|------|------|
| 后端路由 | FastAPI | `api/recommendation.py` — 4 个端点 |
| 推荐算法 | 四因子评分 | 短板 40% + 难度 35% + 兴趣 25% × 新颖度因子 |
| 知识图谱 | NetworkX | KGNode/KGEdge 驱动技能-资料匹配 |
| ORM | SQLAlchemy | MaterialRecommendation |
| 校验 | Pydantic | MaterialItem / RecommendationsResponse / DislikeResponse / ClickRequest |

---

## 模块 14：教师端教学管理后台

### 一、功能流程

#### 14.1 班级管理

```
教师登录 → GET /api/admin/classes → 我的班级列表
创建班级：POST /api/admin/classes（name + description + level_range）→ 生成邀请码
查看学生：GET /api/admin/classes/{id}/students → 学生列表（含等级/学习时长）
学生加入：POST /api/admin/classes/join（invite_code）→ 验证邀请码有效性
```

#### 14.2 作业管理

```
布置作业：POST /api/admin/assignments（class_id + title + content_type + content_ids + due_date）
查看作业：GET /api/admin/assignments → 作业列表（含完成率）
查看提交：GET /api/admin/assignments/{id}/submissions → 提交列表
教师点评：POST /api/admin/submissions/{id}/review（teacher_feedback + teacher_score）
```

#### 14.3 学生报告

```
GET /api/admin/students → 所有班级学生列表
GET /api/admin/students/{id} → 学生详细报告（雷达图+活动时间线+录音回放）
```

| 层次 | 技术 | 说明 |
|------|------|------|
| 后端路由 | FastAPI | `api/admin.py` — 教师端 8 个端点 |
| 角色控制 | require_teacher | 依赖注入验证 role=teacher |
| 服务层 | TeacherService | 班级/作业/学生报告 CRUD |
| ORM | SQLAlchemy | classes / class_students / assignments / assignment_submissions |
| 校验 | Pydantic | ClassItem / AssignmentItem / SubmissionItem / ReviewRequest 等 |

---

## 模块 15：运营管理后台

### 一、功能流程

#### 15.1 用户管理

```
管理员登录 → GET /api/admin/users（page + search + role）→ 分页用户列表
启用/禁用：PUT /api/admin/users/{id}/status（is_active: 0/1）→ 操作日志记录
```

#### 15.2 数据仪表盘

```
GET /api/admin/dashboard → 8 项核心指标：
  DAU / MAU / D1留存 / D7留存 / 总用户 / 活跃用户
  + 用户趋势折线图 + 内容类型分布 + CEFR 等级分布
```

#### 15.3 内容管理

```
GET /api/admin/content/{type}（questions/shadow/materials/dubbing）
  → 按类型查看内容列表
```

#### 15.4 反馈管理

```
GET /api/admin/feedbacks（page + status）→ 反馈列表（分页+状态筛选）
POST /api/admin/feedbacks/{id}/reply（reply）→ 回复反馈
PUT /api/admin/feedbacks/{id}/resolve → 标记已解决
```

| 层次 | 技术 | 说明 |
|------|------|------|
| 后端路由 | FastAPI | `api/admin.py` — 运营端 6 个端点 |
| 角色控制 | require_admin | 依赖注入验证 role=admin |
| 服务层 | AdminService | 用户/仪表盘/内容/反馈管理 |
| ORM | SQLAlchemy | user_profiles / user_feedbacks / admin_logs |
| 校验 | Pydantic | UserListItem / DashboardResponse / FeedbackItem 等 |

---

## 模块 16：智能客服与帮助系统

### 一、功能流程

#### 16.1 文字客服

```
用户输入问题
  ↓
POST /api/help/chat {message, history}
  ↓
HelpService._classify() — LLM 分类(5类)
  ├─ off_topic → 固定话术 "只能回答产品使用和英语学习问题"
  ├─ refund → 固定话术 "请发邮件至 support@lingolab.com"
  ├─ tech_issue → 固定话术 "建议排查后联系人工"
  └─ product_use / study_advice ↓
  ↓
_check_repeat() — 最近3条消息完全相同/高度相似？
  ├─ Yes → escalate=True，LLM 末尾追加人工客服提示
  └─ No → escalate=False
  ↓
_ask_llm() — HELP_SYSTEM_PROMPT + 最近6轮历史 + LLM 生成回复
  ↓
返回 ChatResponse {reply, category, escalate}
```

#### 16.2 语音客服

```
用户录音 → POST /api/help/chat/voice {audio, history}
  ↓
保存临时文件 → WhisperX ASR 转写
  ├─ 转写为空 → "未能识别你的语音，请用文字输入"
  └─ 转写成功 ↓
  ↓
HelpService.chat(transcript, history) — 同文字客服流程
  ↓
返回 ChatResponse {reply, category, escalate, transcript}
```

#### 16.3 FAQ 预设回答

```
用户点击左侧 FAQ 条目
  ↓
前端 selectFaq() — 直接显示预设答案，不调用 API
  ↓
4个分类 11条FAQ：账号相关(3) / 学习功能(3) / 技术问题(3) / 会员与支付(2)
```

### 二、技术架构

```
POST /api/help/chat → HelpService.chat()
  ├─ _classify() → llm._raw_chat(CLASSIFY_PROMPT, temp=0.1)
  ├─ _check_repeat() → 纯字符串匹配
  └─ _ask_llm() → llm._raw_chat_messages(HELP_SYSTEM_PROMPT + history, temp=0.7)

POST /api/help/chat/voice → ASR.transcribe() → HelpService.chat()
```

| 层次 | 技术 | 说明 |
|------|------|------|
| 后端路由 | FastAPI | `api/help.py` — 2 个端点 |
| 服务层 | HelpService | LLM 分类 + 重复检测 + 回复生成 + 降级 |
| LLM | DashScope qwen-plus | 分类(temp=0.1) + 回复(temp=0.7) |
| ASR | WhisperX small | 语音转文字 |
| Schema | Pydantic | ChatRequest / ChatResponse / FaqItem / FaqCategory |
| 前端 | Vue 3 | HelpView.vue — FAQ面板 + 聊天面板 + 语音输入 + 打字动画 |
