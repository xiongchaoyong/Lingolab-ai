# 变更摘要 — 2026-07-05

> 分支：`feat/grammar-check-fixes` | 37 文件 | +1566 / -1063 行

---

## 1. 运营看板增强（5 项）

- **时间范围筛选器** — 快捷预设（7/30/90天）+ 自定义日期选择，后端 `get_dashboard` 支持 `start_date`/`end_date` 参数
- **内容使用排行** — 柱状图展示热门发音内容 + 对话场景 Top N
- **转化漏斗图** — 注册 → 测评 → 首次练习 → 7日留存
- **学习活跃日报** — 后端按日计算 DAU/新增/练习/对话/任务数据
- **CSV 导出** — 日报数据一键导出 Excel
- **Bug 修复** — `DashboardResponse` 中 `TrendPoint` 未导入导致 500；SQLAlchemy `func.distinct()` 子查询列名访问报错
- **Schema** — 新增 `ContentRankItem` / `ConversionFunnel` / `DailyReportItem` / `TrendLabel`

## 2. 教师端新增功能

- **工作台 Dashboard** — 班级数/学生数/待点评/今日活跃 + 班级学生分布图 + 最近作业
- **学生进度趋势图** — 四维度（发音/流利度/语法/词汇）折线图 + 打卡统计柱状图
- **批量布置作业** — 班级选择改为多选，一次布置到多个班级

## 3. 用户反馈入口

- 新增 `FeedbackButton` 浮动按钮（右下角）→ 弹窗提交反馈
- 新增 `POST /api/feedback` 端点
- 新增 `FeedbackCreateRequest` Schema

## 4. 发音评测 — 统一搜索框

- 搜索框输入 Enter → DB 命中直接用，未命中调 LLM 验证英文合法性
- 新增 `POST /api/pronunciation/validate` + `validate_english_text()`
- 去掉独立的「自定义文本」入口

## 5. 内容管理 — 分页

- 后端 `get_content_list` 接口加 `page`/`page_size` 参数，返回 `{items, total}`
- 前端每种内容类型独立分页，每页 10 条

## 6. 代码清理

- 删除「学习小组」模块（前端 + 后端 + 测试 + 种子数据，共 12+ 文件）
- 语法纠错视图重构（语音输入整合）
- 社区 store/api 去小组相关逻辑

---

**涉及文件（37 个）**：`backend/app/services/admin.py`、`backend/app/api/admin.py`、`backend/app/schemas/admin.py`、`frontend/src/views/admin/DashboardView.vue`、`frontend/src/views/admin/ContentManageView.vue`、`frontend/src/views/teacher/TeacherDashboardView.vue`、`frontend/src/views/teacher/StudentReportView.vue`、`frontend/src/views/teacher/HomeworkView.vue`、`frontend/src/components/common/FeedbackButton.vue` 等
